var db = {};

/**
 * MongoDB URI
 * This should be in the correct format. For more help see the MongoDB docs
 * Format: mongodb://{username:password}@{url:port}/{dbname}
 */
db.uri = 'mongodb://localhost:27017/redsauce';

module.exports = db;
